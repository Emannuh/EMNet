[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Note: do NOT use --reuse-db with django-tenants transaction tests
# as schema DDL cannot run inside a savepoint.
